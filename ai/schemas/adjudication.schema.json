{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/evanharmon1/harmon-devkit/ai/schemas/adjudication.schema.json",
  "title": "Dev flow v2 adjudication record — one round's batch",
  "$comment": "Orchestrator-authored, NOT wrapped in result.envelope.schema.json — the envelope is for agent->orchestrator results (see specs/dev-flow-v2.md 'Roles and authority' table); the orchestrator writes this document and run.schema.json directly. One instance covers one round's worth of NEW adjudications, matching how evidence is posted (docs/decisions/0002: 'one comment per round, the moment the round is adjudicated'). The property this family holds a finding to is: exactly one adjudication per finding, per run, ever — enforced in three parts, each by a different mechanism, because no single one of them can see the whole run: (1) within ONE document, the finding_id pattern plus scripts/validate-result-schemas.mjs's checkAdjudicationEntries reject a duplicate finding_id in the same adjudications[] array; (2) a finding_id's own <stage>-r<round> segments must equal THIS document's stage/round (checkAdjudicationIdAttribution, always on, no external context needed — the id grammar makes it local) — this is what stops a finding from one round's document from being silently re-adjudicated under a different round's stage/round claim; (3) across EARLIER round documents of the same run, --known-adjudicated (a JSON array of finding ids already adjudicated by those documents) rejects any entry naming one of them. A run's full adjudicated view is the union of every round's adjudication document for that run. `settlements[]` is deliberately NOT here: specs/dev-flow-v2.md states the run record is 'the only home of mutable run state' and settlements are appended over time as `defer` dispositions resolve during integration, so they live in run.schema.json instead — see its own $comment and ai/schemas/README.md 'Where settlements live'.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "run_id", "stage", "round", "reviewed_head", "adjudications"],
  "properties": {
    "schema": { "const": 2 },
    "run_id": { "type": "string", "minLength": 1 },
    "stage": {
      "description": "review: adjudicates a reviewer pass's findings (--pass names a reviewer envelope). challenge: adjudicates a reviewer pass (a pre-#635 trajectory) OR a challenger pass (#635) — --pass may name either envelope, peeked from its own declared role rather than assumed, since every pass making up one round must still agree with the others on that role (ai/schemas/README.md). integration: adjudicates an integrator payload's findings instead (--pass names an integrator envelope) — its findings carry no reviewer-asserted priority, so the validator's pass cross-check skips the reviewer_priority fidelity comparison for this stage.",
      "enum": ["challenge", "review", "integration"]
    },
    "round": { "type": "integer", "minimum": 1 },
    "reviewed_head": {
      "description": "The head this round's pass(es) reviewed — must equal every adjudicated finding's originating pass's reviewed_head. Checked by the validator's run-context mode, which has the pass documents in hand.",
      "type": "string",
      "pattern": "^[0-9a-f]{40}$"
    },
    "adjudications": {
      "description": "Every finding introduced by this round, each adjudicated exactly once (docs/product/domain.md: 'A finding is adjudicated exactly once, and its adjudication is the only view scripts read'). 'Keyed by finding id' (specs/dev-flow-v2.md) is realized here as an array of entries each carrying its own finding_id, with uniqueness enforced by the validator — the hand-rolled subset validator has no patternProperties or schema-valued additionalProperties, so a JSON object dynamically keyed by finding id is not expressible; an array of tagged objects is the same shape already used for findings[]/checks[]/ac_test_map[] throughout this schema family.",
      "type": "array",
      "items": { "$ref": "#/$defs/adjudicationEntry" }
    }
  },
  "$defs": {
    "adjudicationEntry": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "finding_id",
        "reviewer_priority",
        "adjudicated_priority",
        "disposition",
        "reason",
        "evidence",
        "override"
      ],
      "properties": {
        "finding_id": {
          "type": "string",
          "pattern": "^(challenge|review|integration)-r[1-9][0-9]*-[a-z0-9-]+-[1-9][0-9]*$"
        },
        "reviewer_priority": {
          "description": "A copy of the finding's own reviewer-asserted priority, carried here so reviewer-vs-orchestrator disagreement is measurable from this document alone, without re-reading the pass (specs/dev-flow-v2.md: 'the raw reviewer output is kept so reviewer-vs-orchestrator disagreement can be measured'). The validator's run-context mode still cross-checks it against the originating pass's finding. null for stage integration ONLY: an integrator finding carries no reviewer-asserted priority to copy — there is no reviewer pass — so there is nothing to be null instead of copying incorrectly. Non-null (one of P0-P3) is still required for challenge/review. The validator enforces which is which by stage; this schema only bounds the shape (a string here is always P0-P3, never an arbitrary string).",
          "type": ["string", "null"],
          "if": { "type": "string" },
          "then": { "enum": ["P0", "P1", "P2", "P3"] }
        },
        "adjudicated_priority": {
          "enum": ["P0", "P1", "P2", "P3"]
        },
        "disposition": {
          "description": "Terminal response to the finding, except defer may carry only an adjudicated P2 from challenge/review into integration. P0/P1 remain local-loop gating, and integration findings always require a terminal disposition. These stage/priority conditions are enforced by the semantic validator.",
          "enum": ["fix", "restructure", "delete", "decline", "defer", "file"]
        },
        "reason": {
          "description": "Why this disposition, always required — every adjudication explains itself, not only overrides.",
          "type": "string",
          "minLength": 1
        },
        "evidence": {
          "type": "string",
          "minLength": 1
        },
        "override": {
          "description": "null when adjudicated_priority equals reviewer_priority. Required (a non-null object with a reason) whenever they differ — specs/dev-flow-v2.md's upward-override / disagreement record. This is a cross-field (not cross-document) condition — adjudicated_priority and reviewer_priority are two sibling values within ONE entry — but the subset validator's if/then cannot express 'these two properties are unequal' (JSON Schema has no field-to-field comparison operator), so it is enforced as a semantic check, not a structural keyword. See ai/schemas/README.md.",
          "type": ["object", "null"],
          "additionalProperties": false,
          "required": ["reason"],
          "properties": {
            "reason": { "type": "string", "minLength": 1 }
          }
        },
        "classification": {
          "description": "Optional — AGENTS.md's Codex-findings taxonomy ('classify it: confirmed, plausible but unproven, or false positive') applied to this finding, so a renderer can show the real classification instead of sourcing it from the pass's own asserted `class` (a reviewer-asserted field the orchestrator may not agree with). Absent on older records and whenever the orchestrating agent did not record one; never required, so this addition is backward compatible with every existing adjudication document.",
          "enum": ["confirmed", "plausible", "false-positive"]
        },
        "reference": {
          "description": "Optional — the evidence a human or CI can follow to confirm a disposition decided HERE, at adjudication time, rather than later via run.schema.json's settlements[] (which exists only for a `defer` disposition's eventual fix/decline/file outcome). Same shape as settlements[].reference: without this, a finding declined or filed directly — never deferred — has nowhere in this schema family to record the comment id or issue number a human would need to confirm it. Never required, so this addition is backward compatible with every existing adjudication document. Object-only, unlike override below: this field is genuinely optional (omission already means \"no reference\"), so an explicit null would only be a second, redundant way to say the same thing — never nullable the way override is, where null is itself a required, meaningful answer.",
          "type": "object",
          "additionalProperties": false,
          "required": ["type", "value"],
          "properties": {
            "type": { "enum": ["sha", "comment_id", "issue_number"] },
            "value": { "type": "string", "minLength": 1 }
          }
        }
      }
    }
  }
}
